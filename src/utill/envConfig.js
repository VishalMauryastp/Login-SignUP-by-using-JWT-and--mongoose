// const { VITE_BASE_URL } = import.meta.env;

// const envConfig = {
//   API_URL : VITE_BASE_URL,
// };

// export default envConfig;

// envConfig.js

const { VITE_BASE_URL } = import.meta.env;

export const API_URL = VITE_BASE_URL;
